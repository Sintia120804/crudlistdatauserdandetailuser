<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Crud_berita_user"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- ListMahasiswaActivity -->
        <activity
            android:name=".ListMahasiswaActivity"
            android:exported="false" />

        <!-- DetailMhs -->
        <activity
            android:name=".DetailMhs"
            android:exported="false" />

        <!-- TambahDataUserScreenActivity -->
        <activity
            android:name=".screen.TambahDataUserScreenActivity"
            android:exported="false" />

        <!-- LoginActivity -->
        <activity
            android:name=".screen.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterScreenActivity -->
        <activity
            android:name=".screen.RegisterScreenActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- DetailBerita -->
        <activity
            android:name=".DetailBerita"
            android:exported="false" />
    </application>
</manifest>
